import { red } from '@mui/material/colors'
import { black, white } from '../colors'

export const palette = {
   primary: {
      main: black['A700']
   },
   secondary: {
      main: white['A700']
   },
   error: {
      main: red['50']
   }
}
